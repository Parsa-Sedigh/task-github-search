import {IStatusObj} from "../types/interfaces";

export const STH_WENT_WRONG = 'Something went wrong!';
export const DEFAULT_STATUS_OBJ: IStatusObj = {status: 'IDLE', message: ''};
export const DEFAULT_STATUS_OBJ_LOADING: IStatusObj = {status: 'LOADING', message: ''};
